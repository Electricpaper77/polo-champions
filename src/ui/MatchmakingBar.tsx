import { useEffect, useState } from "react";
export function MatchmakingBar({ onFound }: { onFound: () => void }) { const [seconds,setSeconds]=useState(0); useEffect(()=>{const timer=window.setInterval(()=>setSeconds(s=>s+1),1000);const found=window.setTimeout(onFound,3000);return()=>{window.clearInterval(timer);window.clearTimeout(found)}},[onFound]);const clock=`${String(Math.floor(seconds/60)).padStart(2,"0")}:${String(seconds%60).padStart(2,"0")}`;return <aside className="matchmaking" role="status"><b>SEARCHING FOR MATCH… {clock}</b><span>2V2 QUICK MATCH · {18 + seconds * 3} PLAYERS IN QUEUE</span></aside>; }
