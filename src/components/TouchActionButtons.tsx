import { useEffect, useState } from "react";
export function TouchActionButtons(){const [enabled,setEnabled]=useState(false);useEffect(()=>setEnabled(typeof window!=="undefined"&&"ontouchstart" in window),[]);if(!enabled)return null;const action=(name:"strike"|"gallop",active:boolean)=>window.dispatchEvent(new CustomEvent("polo-touch-actions",{detail:{[name]:active}}));return <div className="touch-actions"><button aria-label="Swing" onPointerDown={()=>action("strike",true)} onPointerUp={()=>action("strike",false)} onPointerCancel={()=>action("strike",false)}>SWING</button><button aria-label="Gallop" onPointerDown={()=>action("gallop",true)} onPointerUp={()=>action("gallop",false)} onPointerCancel={()=>action("gallop",false)}>GALLOP</button></div>}
