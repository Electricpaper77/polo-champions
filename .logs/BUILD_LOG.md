# Build Log

- Initialized vertical-slice plan and project rules.
- Implemented playable field, controllable horse/rider, mallet, physics ball, scoring, HUD, timer, keyboard and gamepad input.
- Dependencies installed. Initial build exposed unused starter files; TypeScript scope has been narrowed to the standalone Vite game source.
- Approved the local bundler script and removed the unused starter PostCSS configuration.
- Production build completed successfully: 593 modules transformed; Vite reported a non-blocking large bundle warning from the 3D runtime.
- Added a Playwright game-load smoke test. The local browser runtime was not yet installed, so its first execution could not launch Chromium.
- Installed Chromium and passed the Playwright smoke test (canvas/HUD loaded with no page errors). Corrected charged-shot release handling before final validation.
- Final release-candidate validation: `npm install`, `npm run build`, and Playwright all passed. Live-browser checks confirmed HUD, timer, pause/resume, restart, and no fatal console errors. This workspace has no `.git` directory, so `git diff --stat`, `git status`, and the requested baseline commit cannot run here.
- Shift/gallop baseline repair: moved motion target calculation into `HorseControls`, with normal riding at 16, Shift gallop at 24, and keyboard braking on X. Shift no longer maps to brake; gamepad LT braking remains unchanged. HUD and README now document the separate X brake control.
- Validation after repair (2026-08-08): `npm.cmd install` passed (npm reported 4 dependency audit vulnerabilities); `npm.cmd run build` passed with TypeScript and Vite (594 modules; existing large-bundle warning); `npm.cmd test` passed (2 Playwright tests). The deterministic regression test verifies normal < gallop target speed, Shift does not reduce speed, Shift release returns to normal, and braking targets zero independently.
- Live-browser validation: canvas loaded with no fatal console errors; exercised keyboard riding/gallop/brake/forehand/backhand/reset inputs plus pause/resume and restart. HUD, timer, camera and baseline field loaded. The console contained only an existing dependency deprecation warning. Goal/ball-physics/shot-strength behavior was not deterministically observable through the available UI, but the existing no-error smoke test passed.
- Known limitation: `C:\Projects\polo-champions` contains no `.git` directory, so repository status/diff/log checks and the requested baseline commit cannot be performed until Git metadata is restored or the project is placed inside its repository.
- Baseline repository setup (2026-08-08): confirmed no existing `.git` directory, then initialized this exact project root with `git init -b main`.
- Deterministic polo mechanics proof: extracted the existing shot impulse calculation and verified that full charge has greater power and vertical impulse than a weak charge, while opposite aim inputs produce opposite horizontal shot directions. Extracted goal-boundary/latch transitions and verified one score on entry, no repeat score while remaining in the goal, and re-arming after the ball exits. Ball reset state is verified as position `(0, 0.65, 0)` with zero velocity and is used by the live Ball reset effect.
- Full baseline revalidation: `npm.cmd install` passed; `npm.cmd run build` passed with TypeScript (595 modules; existing large-bundle warning); `npm.cmd test` passed (4 Playwright tests including live page load with no fatal page errors). The 4 npm audit findings remain noted but are not treated as a baseline failure.
- Game-feel pass (review branch `codex/game-feel`): replaced generic damping with deterministic acceleration, gallop acceleration, coast, and brake rates; added speed-sensitive steering and body lean; added speed-aware camera distance, turn anticipation, and faster high-speed follow response. Mallet now visibly winds up while charging and snaps through impact on release; charge rate and strike cooldown were tuned for a crisper recovery loop.
- Game-feel validation: `npm.cmd run build` passed with TypeScript (595 modules; existing large-bundle warning) and `npm.cmd test` passed (6 Playwright tests). New pure-logic coverage verifies riding response, steering/lean, camera anticipation, and mallet wind-up/impact while preserving all baseline movement, scoring, reset, aiming, and charged-shot proofs. Live browser exercise accepted W, Shift+W, steering, forehand, backhand, X brake, and pause/resume with no fatal console errors.
- B8 fidelity/archetype validation (2026-08-15): `npm.cmd run build` passed with TypeScript and Vite (595 modules; existing large-bundle warning); `npm.cmd test` passed (9 Playwright tests). The deterministic suite covers five gaits, stamina drain/recovery, speed-sensitive turning, rider pose, contact-only/one-hit strikes, charged-shot scaling, and the SPRINTER/ALL_ROUNDER/POWER handling differences.
- B9 visual polish validation (2026-08-15): `npm.cmd run build` passed with TypeScript and Vite (595 modules; existing large-bundle warning); `npm.cmd test` passed (11 Playwright tests). The broadcast HUD uses live player gait, speed, stamina, strike charge, and ball/player radar positions; visual smoke coverage verifies both team header/readability and coat assignment.
- B20 Render deployment pivot validation (2026-08-16): removed the uncommitted GCP Cloud Run deployment script and added a deterministic Render Blueprint for the existing Dockerized WebSocket backend (`polo-champions-backend`, Oregon, free plan, `PORT=8080`). `npm.cmd run build` passed with TypeScript and Vite (618 modules; existing large-bundle warning). An optional cached Docker rebuild was not rerun because Docker Desktop was not running; the B19 Docker target itself is unchanged.
- B21 network resilience validation (2026-08-16): added WebSocket heartbeat timeout handling, entity-preserving AI backfill, server-issued 60-second reconnect tokens, a canonical 500 ms history buffer for lag-compensated strikes and ride-offs, bounded client reconciliation, takeover/reconnect HUD notices, and end-of-match-only telemetry batching. Server syntax checks passed; final `npm.cmd run build` passed with TypeScript and Vite (619 modules; existing large-bundle warning); focused networking validation passed 15 tests; the final full Playwright regression suite passed all 42 tests in 18.4 seconds.
- B26 advanced PC input validation (2026-08-17, baseline `579936f`): registered the full keyboard/mouse control matrix, moved live input capture above suspended GLB assets, added normalized mouse aiming independent of steering, `Shift+Space` / `R+LMB` / `F+RMB` chorded actions, and bounded 5-20 m wheel zoom with middle-button recentering. `node --check server/realtime-server.mjs` passed; `npm.cmd run build` passed with TypeScript and Vite (621 modules, 43.47 seconds; existing large-bundle warning); focused input/polo/network validation passed 30/30 tests in 1.7 minutes; the final serialized full Playwright regression suite passed 47/47 tests in 2.0 minutes; and in-app browser QA verified decoupled aim, pause-safe mouse capture, resume behavior, HUD fit at 1280x720, and zero console errors. `git diff --check` passed.
- Gameplay stability/polish repair (2026-08-17, baseline `31da825`): replaced the client and authoritative server's eleven-bot all-to-ball convergence with an intent-gated kickoff, one chaser per team, formation-relative support lanes, bounded acceleration/turning, team-directed bot clearances, and deterministic mass-weighted separation for all 66 rider pairs. The measured 12-rider formation has symmetric team centroids at +/-17.833 m, 12.042 m initial minimum spacing, and a 2.6 m enforced collision floor; score ownership, server-synchronized restart/reset, live radar data, ball damping, and horse/rider forward orientation were corrected. `node --check server/realtime-server.mjs` passed; the final `npm.cmd run build` passed with TypeScript and Vite (621 modules, 27.06 seconds; existing large-bundle warning); focused gameplay/polo/network validation passed 32/32 tests; the final full suite passed 54/54 tests in 2.3 minutes; and in-app browser QA verified a stationary 07:00 kickoff, 12 separated radar/rider positions, rear chase presentation, 1280x720 HUD fit, and zero console errors.
